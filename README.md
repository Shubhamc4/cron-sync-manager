# cron-sync-manager
A lightweight, idempotent Bash script to manage Ubuntu crontabs across different projects. Define your jobs in a simple config file and let the engine handle the rest without duplicates.
